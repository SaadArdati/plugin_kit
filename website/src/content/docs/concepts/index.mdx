---
title: Core Concepts
description: A map of the runtime's mental model. Plugins, services, events, sessions, settings, and the metadata that holds them together.
---

import { Card, CardGrid, LinkCard } from '@astrojs/starlight/components';

The Concepts section is the mental-model side of Plugin Kit. Each page covers one piece of the runtime's vocabulary: what it is, how it fits with the others, and the design choices behind it. The [reference](/reference/) is for looking up signatures; [guides](/guides/) are for action-oriented walkthroughs; the pages here are for understanding the model that makes the rest cohere.

## How the pieces fit

A **plugin** has an identity and a lifecycle. The runtime decides which plugins are enabled, calls their `register` hook to collect services into the registry, then calls `attach` to start them. **Plugin services** are the stateful classes a plugin owns; the registry stamps identity onto them and injects settings when they resolve.

The **service registry** is where competition happens. Two plugins can register for the same slot; the higher priority wins, and the loser stays reachable as a fallback. **Capabilities** are metadata that travel with each registration so host code can inspect what a slot supports without building anything.

The **event bus** is how plugins coordinate without holding references to each other. Typed events flow in priority order; typed requests fall through handlers until one returns a non-null response. **Event Patterns**, the page next to event bus, covers the patterns for using all of this from inside plugin code rather than the model itself.

The **runtime** owns the global scope and spawns **sessions**. A session is an isolated execution scope with its own registry and bus; opening a second session never disturbs the first. The manager wraps the runtime with a settings snapshot and a change stream so the host can hand it new `RuntimeSettings` and have it converge.

**Custom contexts** are the optional extension point: when your domain has fields that every plugin in a session should see (the current document, the active user, a tool registry), you subclass `SessionPluginContext` and supply a factory.

## The pages

<CardGrid>
  <LinkCard
    title="Plugins"
    description="The unit the runtime cares about. Identity, lifecycle, scope (Global vs Session), feature flags, and the four hooks every plugin can override."
    href="/concepts/plugins/"
  />
  <LinkCard
    title="Plugin Services"
    description="Where stateful behavior lives. `PluginService` for typed config, `StatefulPluginService` for session-bound subscriptions and tracked cleanup."
    href="/concepts/plugin-services/"
  />
  <LinkCard
    title="Service Registry"
    description="Priority-based DI where multiple plugins can claim the same slot. `resolveAfter` for fallback layering. Wildcard overrides and namespaces."
    href="/concepts/service-registry/"
  />
  <LinkCard
    title="Capabilities"
    description="Metadata tags attached to registrations so host code can discover what a service supports without instantiating it."
    href="/concepts/capabilities/"
  />
  <LinkCard
    title="Configuration"
    description="`ConfigNode`, the typed read-only view a service uses to consume injected settings. Coercion rules and what the registry guarantees."
    href="/concepts/configuration/"
  />
  <LinkCard
    title="Event Bus"
    description="The model. Typed handlers, priority order, the envelope, request/response, identifier scoping, cross-scope routing."
    href="/concepts/event-bus/"
  />
  <LinkCard
    title="Event Patterns"
    description="Patterns for using the bus from inside plugin code. Designing event types, the pre-commit interception pattern, common pitfalls."
    href="/concepts/events/"
  />
  <LinkCard
    title="Runtime"
    description="The lifecycle engine. `PluginRuntime`, `PluginRuntimeManager`, settings reconciliation, enablement precedence."
    href="/concepts/runtime/"
  />
  <LinkCard
    title="Sessions"
    description="Per-session isolation, the global vs session bus split, and explicit cross-scope routing."
    href="/concepts/sessions/"
  />
  <LinkCard
    title="Custom Contexts"
    description="Extending `PluginContext` with domain-specific fields like the current document, user, or tool registry. When to do it and when not to."
    href="/concepts/custom-context/"
  />
</CardGrid>

The sidebar order is the order most readers find useful (smallest unit outward to the things that move plugins around), but every page links to the others it depends on.

## Where to next

<CardGrid>
  <LinkCard
    title="API reference"
    description="Curated reference for the public types, organized by intent: plugins, registry, bus, settings, dialog."
    href="/reference/"
  />
  <LinkCard
    title="Guides"
    description="Action-oriented walkthroughs. Add a plugin, wire it into Flutter, configure it, log it, test it."
    href="/guides/"
  />
  <LinkCard
    title="Architecture"
    description="The internal type hierarchy, dispatch model, and design rationale, for contributors and curious readers."
    href="/reference/architecture/"
  />
</CardGrid>
