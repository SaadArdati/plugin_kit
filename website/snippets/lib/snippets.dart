/// One-off Dart snippets for the website docs.
///
/// Lives under website/ because it serves only docs. Each snippet file
/// exposes regions via `// #docregion <name>` markers; the website's MDX
/// pages and the package READMEs read those regions at build time.
library;
