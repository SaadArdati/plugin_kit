tags:
  goldens:
    description: >-
      Visual golden tests. Skipped in CI because golden rendering is
      environment-sensitive. The committed PNGs are for rendering doc images
      on the website; run `flutter test --update-goldens test/golden_test.dart`
      locally after any UI change.
