name: model_embassy
description: >
  Multi-provider AI model routing via the Visa Office pattern.
  Teaches plugin_kit through a real-world credential routing system.
publish_to: none
resolution: workspace

environment:
  sdk: ">=3.10.0 <4.0.0"

dependencies:
  plugin_kit:
    path: ../../packages/plugin_kit

dev_dependencies:
  lints: ^6.1.0
